We want to optimize the parameters of `atomic_solver/` via gradient descent.

Read the interface contract `atomic_solver/docs/spec/optimizer_interface.md` and propose a rough idea how.
For now this is just a first brainstorming session.
